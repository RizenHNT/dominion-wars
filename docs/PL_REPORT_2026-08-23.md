# 2026-08-23 PL 汇报

> MiniMax (temporary PL) 2026-08-23 closeout
> 作者：MiniMax（临时 PL · DeepSeek V4 履职） · 本日为复核 + 决策落档 + QA 验收 + 派工日（无新实现，无生产代码改动）
> 仓库状态：main @ ebb25e8 · ahead 5 · 未提交变更 181 files（72 modified + 109 untracked）· 未 push · 未 release

---

## 1. 今日交付概要

本日为纯复核日，无新增实现。PL 完成两项既定任务：

1. **跟踪 5 项阻断级人类决策**（统领形态 / 手动下载 / 破城胜负 / 古木512 / 深海潮位）——确认全部仍为 OPEN，无人类回复记录。
2. **推进 winParam 跨栈断裂定夺**——完成端到端事实核验，给出 PL 推荐意见（见 §4）。

Codex 8/20–8/21 交付（非 Unity 运行闭环收口 + Unity runtime slice 实机验收）已在前次 PL 复核中结案，本日无新增变更。

---

## 2. 复核结果

### 2.1 仓库状态复核

| 检查项 | 结论 |
|---|---|
| 分支 | main @ ebb25e8（2026-08-16 最后提交） |
| 领先远程 | 5 commits |
| 未提交变更 | 181 files（72 modified + 109 untracked） |
| Mailbox 行数 | 388 行（<400，未触发压缩） |
| 上次提交后 | 无新 commit（Codex 多轮交付均未 commit，保留 dirty） |

### 2.2 winParam 跨栈断裂 — 事实核验（PL 独立验证，非引用）

| 层 | 行为 | 位置 |
|---|---|---|
| Java CardDef | 只读 `winParam`（默认 0），**无 winAmount 兼容** | CardDef.java:103 |
| Java Game | `opp.totalDiscarded >= ld.winParam`，winParam=0 时恒真 → 首领秒胜 | Game.java:971 |
| C# CardCatalog | 读 `winParam`，缺失则回退 `winAmount`（双字段兼容） | CardCatalog.cs:140-146 |
| .NET 内部模型 | 已统一 `LeaderWinParam` 命名 | CardDefinition.cs / ContractDtos.cs / EngineProjectionAdapter.cs |
| Schema | LeaderDef 同时允许 `winAmount` 与 `winParam`（均 integer 0-999），required 仅 `winCondition` | cards.schema.json `$defs.LeaderDef` |
| 当前引擎数据 | `data/cards/*.json` 已用 **winParam**（machine_alpha=15 / sea_leader=18 / wood_leader=7），winAmount 计数为 0 | data/cards/machine.json / sea.json / wood.json |
| 设计包 bundle | 用 **winAmount**（machine=6 / sea=12 / wood=512），未导入引擎数据 | docs/卡牌设计包_2026-08-15/bundle_v2.json |

**结论**：断裂为**潜在风险**，非当前活跃故障。当前 `data/cards/*.json` 引擎数据全部使用 winParam，与 Java/C#/.NET 均一致，现有链路无 bug。风险仅在 **bundle（winAmount）未来导入引擎数据**时触发：若导入路径是 Java（历史库存）则 winParam=0 → 秒胜。C# 路径因双字段兼容可正常兜底。

---

## 3. 已知阻塞

| 阻塞项 | 状态 |
|---|---|
| 5 项阻断级人类决策 | 🔴 全部 OPEN（详见 §4） |
| winParam 字段定夺 | 🟡 已核验、待人类/Codex 拍板 |
| Codex 冻结接口清单（mailbox L381） | 🟡 SET_AMBUSH / COMMIT-PULL-ROLLBACK / commit queue 投影 / 新关键词 / 机械费用接线 |
| 181 files 未提交 + ahead 5 | 🟡 待 Codex 回场或人类决策后收档 |

---

## 4. 需人类决策的事项

### 🔴 阻断级（6 项，其中第 1–5 项为既定清单，第 6 项为本日新增推进）

| # | 事项 | 选项 | PL 推荐 |
|---|---|---|---|
| 1 | 统领形态 | A: 洗入牌库 / B: 固定在场 | 待定（需人类） |
| 2 | 手动下载语义 | 见 HUMAN_DECISIONS_2026-08-16 | 待定 |
| 3 | 破城胜负判定 | 见 HUMAN_DECISIONS_2026-08-16 | 待定 |
| 4 | 古木 512 阈值 | 见 HUMAN_DECISIONS_2026-08-16 | 待定 |
| 5 | 深海潮位 | 见 HUMAN_DECISIONS_2026-08-16 | 待定 |
| 6 | **winParam 跨栈字段** | ① 数据补 winParam（双写，最简单）/ ② Java 加 winAmount 兼容 / ③ schema 统一单字段 winParam | **③（推荐）** |

### winParam 推荐意见（PL 立场，最终以人类/Codex 定夺为准）

**推荐方案 ③：schema 统一单字段 `winParam`**，理由：

1. **.NET 为当前运行权威**，Java 已降级为历史库存（不再新增行为）；.NET 内部已用 `LeaderWinParam` 命名。
2. 当前引擎数据 `data/cards/*.json` 已全部使用 `winParam`，schema 统一后零迁移成本。
3. 未来 bundle 导入时需将 `winAmount` 翻译为 `winParam`（数据层转换，无运行时代码改动）；bundle 本身是设计源文件，暂不改动，仅作为导入约定。
4. 可选兜底：保留 C# `winAmount` 回退 1 个版本，避免历史数据意外破坏；Java 因已冻结不必补兼容。

**待办移交**：
- **人类**：确认 schema 是否移除 `winAmount`（数据契约变更属人类权限）。
- **Codex**：schema 统一为 `winParam` 并同步导入工具（数据层，非运行时）。

---

## 5. 当前仓库状态

- **未提交变更**：181 files（72 modified + 109 untracked）
- **领先远程分支**：5 commits
- **未 push、未 release**

下一步：等待人类负责人对 §4 阻断级事项给出方向。一旦人类拍板 5 项 + winParam，DeepSeek 即可执行全局修复（幽灵 P' 145 / BUFF:SELF 47 / 防御回补），Codex 同步收档 181 files。

---

## 6. 人类决策已收到（2026-08-23 10:23）— 阻塞部分解除

人类负责人已对 5 项阻断决策作出答复（正文照录 + PL 转译为可实施规则）：

| # | 事项 | 人类答复（照录） | 转译（可实施规则） |
|---|---|---|---|
| 1 | 统领形态 | 抽上来只要任何方式离开卡组就直接出场，之后按是否被惩罚自动结算费用；主动出场按自己打出算，惩罚按惩罚算；后续所有流程与你使用卡牌相同（除非该卡本身有特殊规则） | 统领离开卡组（含抽牌/任何方式）即**直接进入战场，不入手牌**；费用按主动/惩罚自动结算：主动出场=按我方正常打出结算，被惩罚=按惩罚结算；后续流程与普通出牌一致；卡牌自身特殊规则优先。 |
| 2 | 手动下载 | 云端发光，点击最顶端的卡牌可以拉一个箭头，选择下载对象，支付惩罚，结算该最顶端的下载效果 | 云端顶端可**手动下载**：云端高亮提示 → 选中顶端卡 → 拖出箭头选择下载目标 → 支付惩罚 → 结算顶端卡的下载效果。 |
| 3 | 破城胜负 | 规则书有。正常破城：抽牌计数扣到还有一次，对方首领强制出场（普通方式，我方抽牌），并且我方有增益 | 破城时防守方抽牌计数扣至剩 1；对方首领经**普通抽牌方式**强制入场；进攻方获得增益。 |
| 4 | 古木512阈值 | 数值策划决定 | 委托给数值策划；PL 需产出建议值供确认，**未确认前不实施**。 |
| 5 | 深海潮位 | 游戏策划决定 | 委托给游戏策划；PL 需产出规则提案供确认，**未确认前不实施**。 |

**影响**：
- 事项 1–3 已冻结，可移交 Codex 实施；其中与全局修复（幽灵 P' 145 / BUFF:SELF 47 / 防御回补）相关的部分解除阻塞。
- 事项 4–5 委托给策划角色，PL 将产出建议（数值/规则提案）供确认后再冻结。
- winParam 字段定夺（§4 第 6 项）仍待 Codex/人类确认。

### 6.1 事项 4–5 委托草案（PL 建议，待数值/游戏策划确认后冻结）

| # | 事项 | PL 草案（源自 HUMAN_DECISIONS_2026-08-16 分析） |
|---|---|---|
| 4 | 古木 512 阈值 | **草案 A：512 阈值 + 疯长仅统领结算**（2^9 主题，修 BUFF 目标 SELF→FRIENDLY 后 ×8 仅 11 步达 528）。备选：384 + 疯长统领 only（T10-12 可达，更平衡）。最终数值由数值策划定。 |
| 5 | 深海潮位 | **草案 A：本包不做潮位**（保持现状，海以弃牌+潮蚀为轴，潮位留扩展包；已确认 0 张为预期）。备选：B 冻结基础潮位（补 5-10 张）/ C 用"潮汐债"替代（统领候选机制，已部分落地 3 张）。最终规则由游戏策划定。 |

---

## 7. WBS 状态更新 + 派工（2026-08-23）

### 7.1 WBS 快照（已更新 `docs/PROGRESS_WBS.md`）

| 模块 | 完成度 | 状态 | 一句话 |
|---|---|---|---|
| 1 引擎 / 2 数据 / 3 规则 | 100% | ✅ | C# 引擎与数据契约全绿 |
| 4 适配层 | 85% | 🟢 | 4.4-4.7 完成 |
| 5 测试矩阵 | 99% | 🟢 | .NET 428/428 + Unity 实机 8/21 验收过（EditMode 24/24 + PlayMode 2/2 + Windows build PASS） |
| 6 前端 / Unity 渲染 | 15% | 🟡 | **6.0 壳已过；6.1-6.5（牌桌/对战 UI/目标/惩罚链/动效）仍欠 = "能玩"最大缺口** |
| 7 资产 / 美术 | 0% | 🔴 | 91 卡专属图待人类/GPT Web/美术 |
| 8 运行时 QA / 9 自动化 | 10% / 20% | 🟡 | 性能/多分辨率/无障碍未做；relay 未跑通 |

**项目总进度 ~50%**（引擎侧高完成，前端与美术是剩余主体）。

### 7.2 派工决定（mailbox 已发两条 🔴 通知）

| 谁 | 开工做什么 | WBS | 依赖 |
|---|---|---|---|
| **Codex** | ① 统领直接出场 / ② 手动下载 / ③ 破城（引擎+数据+事件）＋ 全局修复（幽灵 P'145 / BUFF:SELF 47 / 防御回补） | 10.11.1/2/3 + 10.11.7 | ②③ 实机目标 UI 依赖前端 6.3，先离线 |
| **DeepSeek** | ④ 古木512 数值提案（确认 PL 草案 A）+ ⑤ 潮位规则提案 ＋ 为 ①-③ 起草验收测试 | 10.11.4/5 + ①-③ 测试 | 无 |
| **人类** | 确认 ③ 的"我方抽牌"理解 ＋ winParam（10.11.6）＋ 前端视觉方向（解锁 6.1-6.5） | — | — |

### 7.3 下一步（开工路径）

- **人工 attended 方式**（无需 relay）：Codex/DeepSeek 已可从 mailbox 领取 ①-③ + 全局修复 + ④⑤ 提案。
- **无人值守 relay 方式**：需人类批准新日目标（当前 DAILY_GOAL 仍是 8/13 过期范围）→ 写入 DAILY_GOAL 置 READY → `start-relay.ps1 -PlanOnly -ApprovedByHuman` → 人类批准计划 → `-ApprovedPlan -ApprovedByHuman`。
- 人类唯一必须拍板的实施前事项：③ 的"我方抽牌"含义、winParam 方案（PL 建议 schema 统一为 winParam）、以及是否走 relay。

---

## 8. QA 交付验收（2026-08-23 11:19）

DeepSeek 策划/QA 已完成派工交付：`docs/QA_PROPOSAL_AND_ACCEPTANCE_2026-08-23.md`（④⑤ 提案 + ①-③ 验收用例），并自行回填 WBS 10.11.1-5。

### 8.1 PL 独立复核（非引用）

| 项 | QA 主张 | PL 复核 | 结论 |
|---|---|---|---|
| 引擎成长公式 | `(baseAmount + rootLayers) × 2^min(3, rampantLayers)`，HP 累加 | `EffectRuntime.Combat.cs` `ApplyGrowth` 逐字吻合；HP 用 `SaturatingAdd`（累加）| ✅ |
| growth 自动封印 | growth 生效后 Sealed=true / Attack=0 / Shield=false | 代码 L188-192 吻合 | ✅ |
| 木统领现状 | NO_DAMAGE_TURNS_GE / winParam 7，enter SUMMON2 / punish PROTECT_TURN | `data/cards/wood.json` 实测吻合 | ✅ |
| ④ 可达性 | ×8 疯长 10 步达 521（≈5-6 回合） | 公式推演合理，与 PL 草案量级一致 | ✅ |
| .NET 基线 | 419/419 | **实测 435/435（2026-08-23，`dotnet test` Release）** | ⚠️ 基线更正为 435 |

### 8.2 结论

- **④⑤ 设计已确认**（设计角色 DeepSeek 定稿）：④ = 512 + 疯长仅统领结算；⑤ = 本包不做潮位。落地障碍 G1-G5 已列明交 Codex。
- **①-③ 验收基线就绪**：TC1-1~1-6 / TC2-1~2-5 / TC3-1~3-5，Codex 实现后按此验收（②③ 实机部分依赖前端 6.3）。
- **合并实施指令已发 Codex**（mailbox 🔴）：①-③ + ④ + ⑤ + 10.11.7 全局修复 + 10.11.6 winParam 确认。
- 剩余人类确认点不变：③"我方抽牌"理解、winParam 方案、前端视觉方向、是否走 relay。

---

## 9. Codex 实现完成（2026-08-23 12:31）

Codex 已完成本轮实现，交付 `docs/CODEX_IMPLEMENTATION_REPORT_2026-08-23.md`，mailbox 🟢 handoff。

### 9.1 已完成内容（WBS 10.11.1-10.11.5 + 机械 10.11.7）

| WBS | 内容 | 落地情况 |
|---|---|---|
| 10.11.1 | 统领离组即直接出场 | 三区直接出场路径 + ForceLeaderOut 统一 + fail-closed |
| 10.11.2 | 手动下载引擎 | PULL 法律动作 + fail-closed 处理器 + PULL_DECLARED/CARD_PULLED 事件 |
| 10.11.3 | 破城 | 单次结算 + CycleWinCount=max(9) + 对立首领强制出场 + minion 双统领即时胜 |
| 10.11.4 | 古木 512 | ADD_RAMPANT/ADD_ROOT + wood_leader→GIANT_HEALTH_GE/512 + 移除普通木卡疯长 tag |
| 10.11.5 | 深海潮位 | 清理 sea_leader bundle 潮位残留；未引入未冻结 action |
| 10.11.7 | 全局修复（机械部分） | AMBUSH punishCost:0（23 修正）+ BUFF SELF→FRIENDLY_MINION（54 处/47 卡）+ CardCatalog 显式字段保留 |

### 9.2 PL 独立复测（非引用）

| 项 | Codex 报告 | PL 复测 | 结论 |
|---|---|---|---|
| .NET Release 基线 | 456/456 | **实测 `dotnet test` Release = 456/456 通过 0 失败** | ✅ 一致 |
| wood_leader 数据 | GIANT_HEALTH_GE/512 + enter SUMMON2+ADD_RAMPANT1 + punish PROTECT_TURN+ADD_RAMPANT2 | `data/cards/wood.json` 逐字吻合 | ✅ |
| 引擎新增 action/事件 | ADD_RAMPANT/ADD_ROOT/PULL 事件/CycleWinCount | `src/Engine` 源码命中 | ✅ |

### 9.3 遗留 HUMAN_REQUIRED（需人类/相关角色拍板）

1. **10.11.7 幽灵 P'122 张**（非 AMBUSH 且 punishCost>0 但无惩罚效果）：Wood 32 / Sea 35 / Flame 38 / Neutral 17。清成本 vs 补效果 = 设计/平衡决策，Codex 未臆测。
2. **10.11.6 winParam 迁移**：bundle 仍用 winAmount，schema/loader 现双字段兼容；统一为 winParam 的显式迁移决策未做。
3. **防御回补**：卡数、数值、破城 heal/buff 语义需设计决策后方可改生产数据。

### 9.4 下一步

- **DeepSeek**：用 QA_PROPOSAL_AND_ACCEPTANCE_2026-08-23.md §三 用例（TC1/TC2/TC3）对 ①-③ 做独立验收（mailbox 🔴 已派）。
- **人类**：③"我方抽牌"语义最终确认；winParam（10.11.6）迁移拍板；10.11.7 幽灵 P' 与防御回补设计决策；前端视觉方向（解锁 6.1-6.5）。
- **Unity 实机**：本轮引擎改动需 Hub/许可证人工跑一次 GUI 回归（Codex 离线未跑）。

---

## 10. DeepSeek 独立验收通过（2026-08-23 Round 1）

DeepSeek 已完成对 ①-③ 的独立验收，交付 `docs/QA_REPORT_2026-08-23_ROUND1.md`（mailbox 🟢）。

### 10.1 QA 独立实测（非引用）

| 项 | 实测 | 结论 |
|---|---|---|
| .NET Release 基线 | **456/456 通过 0 失败** | ✅ 与 Codex/PL 复测一致 |
| 破城（TC3-1~3-5） | 6/6 通过 | ✅ |
| 统领直接出场（TC1-1~1-6） | 43/43，无 FAIL | ✅（TC1-2/1-5/1-6 建议补直接断言，非缺陷） |
| 手动下载引擎（TC2-1~2-5） | 13/13，无 FAIL | ✅（TC2-2 实机拖拽依赖 6.3 跳过；TC2-3 建议补支付断言） |
| 木方 512（④） | 3/3 | ✅ |
| Java / 卡 / 牌组 / 清单 / schema | 38/38 / 91/91 / 4/4 / 320/320 / 4（11/6） | ✅ |

**结论：15 个验收用例无 FAIL，本轮无必须修的缺陷**。仅 3-4 条低优先级覆盖性增强建议（已写入 QA 报告，可进 mailbox）。

### 10.2 遗留（需人类/角色拍板后才可进下一步）

1. **③ 破城"增益归属"**：按 PL 理解实现为"增益记在破城方（CycleWinCount=max(现有,9)）+ 对方首领普通抽牌强制出场"。QA 已标注此语义**人类未最终确认**，后续内容包（10.11.6）会踩坑。
2. **② 下载惩罚模型**：引擎对 downloadCost>0 fail-closed；当前 22 张机械卡 downloadCost 全 0，不阻塞现卡；惩罚来源/数值需人类或策划定。
3. **TC1-2"费用"语义**：统领主动出场是否扣资源，需确认。
4. **Codex 遗留 HUMAN_REQUIRED**：10.11.7 幽灵 P'122 张、10.11.6 winParam 迁移、防御回补设计决策。

### 10.3 下一步

- WBS 10.11.1-10.11.5 + 机械 10.11.7 已回填 🟢 已实现已验收；派工顺序更新为"Codex 完成 → DeepSeek 验收通过 → 等人类拍板"。
- **人类唯一阻塞**：③ 增益归属 / ② 惩罚模型 / 10.11.6 winParam / 10.11.7 遗留设计 / 前端视觉方向。拍板后可进 10.11.6 迁移与前端 6.1-6.5（"能玩"最大缺口）。
- **Unity 实机**：本轮引擎改动仍待 Hub/许可证人工跑一次 GUI 回归。

---

## 11. Codex 补充交付：术语审计 + 补测（2026-08-23 13:18）

Codex 对 QA Round 1 反馈做补充收尾，交付 mailbox 🟢。

### 11.1 Codex 本轮内容

1. **术语审计**：未发现新增未批准机制；正式动作统一为 `ADD_ROOT`/`ADD_RAMPANT`/`PULL`，契约事件统一为 `PULL_DECLARED`/`CARD_PULLED`，不再混称。
2. **“幽灵 P'”改称**：改为“punishCost>0 且无 punishEffects 的条目”，可能是合法纯费用设计，仍待策划裁定（非缺陷表述）。
3. **补测试**：补齐 QA 建议的 5 个直接断言（伏击统领进伏击区 / 非随从统领不可被杀或移走 / 空云端不广告 PULL / ForceLeaderOut 手牌优先墓地铁底）。全量 .NET **461/461**。

### 11.2 PL 独立复测（非引用）

| 项 | Codex 报告 | PL 复测 | 结论 |
|---|---|---|---|
| .NET Release 基线 | 461/461 | **实测 461/461 通过 0 失败** | ✅ 一致 |
| 补测断言 | 5 个直接断言（伏击区/非随从统领/空云端/ForceLeaderOut） | 已并入全量 461 通过 | ✅ |
| 术语 | ADD_ROOT/ADD_RAMPANT/PULL + PULL_DECLARED/CARD_PULLED | 与 PL 派工/QA 报告一致 | ✅ |

### 11.3 PL 文档同步（RULES.md §9.1 修正）

Codex 指出 RULES.md §9.1 仍写“叫出破城方自己的统领”，与人类裁决（叫出**对方**首领，普通抽牌方式强制入场）不一致。PL 已同步修正 `docs/RULES.md` 行 136：改为“叫出对方首领（经普通抽牌方式强制入场，沿用统领离开卡组直接出场的统一路径）”，与 PL_REPORT §6 人类裁决、Codex 实现、QA 验收一致。

### 11.4 遗留（不变）

- **HUMAN_REQUIRED**：122 非 AMBUSH punishCost>0 无惩罚效果条目（清成本 vs 补效果）；10.11.6 winAmount→winParam 迁移；防御回补卡数/数值/语义。
- **ENVIRONMENT_BLOCKED**：Unity 批处理因缺 `com.unity.editor.headless` 许可退出 198，EditMode/PlayMode/Windows 验证需人工 GUI 复验（2026-08-21 旧证据不构成本批新改动验证）。
- **人类阻塞**：③ 破城增益归属（CycleWinCount=max(现有,9) 归属破城方）未最终确认 + ② 下载惩罚模型 + 前端视觉方向。
